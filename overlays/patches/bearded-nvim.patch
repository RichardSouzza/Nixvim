diff --git a/lua/bearded/highlights.lua b/lua/bearded/highlights.lua
index 5e7278f..e7a518d 100644
--- a/lua/bearded/highlights.lua
+++ b/lua/bearded/highlights.lua
@@ -97,8 +97,8 @@ local function syntax_groups(colors, opts)
     SpecialComment = { fg = colors.dim, italic = italic },
     Debug = { fg = colors.red },
     ["@comment"] = { fg = colors.dim, italic = italic },
-    ["@variable"] = { fg = colors.salmon },
-    ["@variable.member"] = { fg = colors.orange },
+    ["@variable"] = { fg = colors.fg },
+    ["@variable.member"] = { fg = colors.salmon },
     ["@variable.parameter"] = { fg = colors.pink },
     ["@variable.builtin"] = { fg = colors.orange, italic = italic },
     ["@field"] = { fg = colors.pink },
@@ -109,9 +109,9 @@ local function syntax_groups(colors, opts)
     ["@number"] = { fg = colors.orange },
     ["@boolean"] = { fg = colors.red },
     ["@string"] = { fg = colors.green },
-    ["@string.escape"] = { fg = colors.red },
-    ["@string.special"] = { fg = colors.red },
-    ["@string.regexp"] = { fg = colors.red },
+    ["@string.escape"] = { fg = colors.dim },
+    ["@string.special"] = { fg = colors.orange },
+    ["@string.regexp"] = { fg = colors.orange },
     ["@character"] = { fg = colors.green },
     ["@character.special"] = { fg = colors.teal },
     ["@constructor"] = { fg = colors.purple },
@@ -136,7 +136,7 @@ local function syntax_groups(colors, opts)
     ["@namespace"] = { fg = colors.teal },
     ["@module"] = { fg = colors.teal },
     ["@punctuation"] = { fg = colors.dim },
-    ["@punctuation.bracket"] = { fg = colors.dim },
+    ["@punctuation.bracket"] = { fg = colors.teal },
     ["@punctuation.delimiter"] = { fg = colors.dim },
     ["@tag"] = { fg = colors.purple },
     ["@tag.attribute"] = { fg = colors.orange, italic = italic },
@@ -159,7 +159,7 @@ local function syntax_groups(colors, opts)
     ["@lsp.type.parameter"] = { fg = colors.pink },
     ["@lsp.type.property"] = { fg = colors.orange },
     ["@lsp.typemod.property.declaration"] = { fg = colors.fg },
-    ["@lsp.type.variable"] = { fg = colors.salmon },
+    ["@lsp.type.variable"] = { fg = colors.fg },
     ["@lsp.typemod.variable.defaultLibrary"] = { fg = colors.teal },
   }
 end
@@ -197,8 +197,8 @@ local function ui_groups(ui, colors, levels, opts)
   local italic = opts.italic ~= false
   local bold = opts.bold ~= false
   local groups = {
-    Normal = { fg = fg, bg = bg },
-    NormalNC = { fg = fg, bg = bg },
+    Normal = { fg = fg, bg = bg_alt },
+    NormalNC = { fg = fg, bg = bg_alt },
     NormalFloat = { fg = fg, bg = bg_alt },
     FloatBorder = { fg = dim, bg = bg_alt },
     FloatTitle = { fg = primary, bg = bg_alt, bold = bold },
@@ -210,7 +210,7 @@ local function ui_groups(ui, colors, levels, opts)
       bg = bg_mid,
       bold = bold,
     },
-    LineNr = { fg = bg == "NONE" and dim or blend_hex(fg, bg, 0.25), bg = bg_mid },
+    LineNr = { fg = bg == "NONE" and dim or blend_hex(fg, bg, 0.25), bg = bg_alt },
     Visual = { bg = bg == "NONE" and (ui.primaryalt or "#444444") or blend_hex(primary, bg, 0.3) },
     Search = { fg = bg, bg = levels.warning or colors.orange },
     IncSearch = { fg = bg, bg = levels.info or colors.blue, bold = bold },
@@ -222,7 +222,7 @@ local function ui_groups(ui, colors, levels, opts)
     StatusLineNC = { fg = dim, bg = ui.uibackgroundalt or bg_alt },
     VertSplit = { fg = ui.border or dim, bg = bg },
     WinSeparator = { fg = ui.border or dim, bg = bg },
-    SignColumn = { fg = dim, bg = bg_mid },
+    SignColumn = { fg = dim, bg = bg_alt },
     ColorColumn = { bg = bg_mid },
     Folded = { fg = dim, bg = bg_mid },
     FoldColumn = { fg = dim, bg = bg_mid },
@@ -241,10 +241,10 @@ local function ui_groups(ui, colors, levels, opts)
     TabLine = { fg = dim, bg = bg_alt },
     TabLineSel = { fg = fg, bg = bg_mid, bold = bold },
     TabLineFill = { fg = dim, bg = bg_alt },
-    DiffAdd = { bg = "#2cfc82", fg = levels.success or colors.green },
-    DiffDelete = { bg = "#f44a24", fg = levels.danger or colors.red },
-    DiffChange = { bg = "#69c3ff", fg = colors.blue },
-    DiffText = { bg = "#69c3ff", fg = colors.blue, bold = bold },
+    DiffAdd = { bg = ui.background, fg = levels.success or colors.green },
+    DiffDelete = { bg = ui.background, fg = levels.danger or colors.red },
+    DiffChange = { bg = ui.background, fg = colors.blue },
+    DiffText = { bg = ui.background, fg = colors.blue, bold = bold },
     StatusLineTerm = { fg = fg, bg = ui.uibackgroundalt or bg_alt },
     StatusLineTermNC = { fg = dim, bg = ui.uibackgroundalt or bg_alt },
   }
diff --git a/lua/bearded/plugins/lualine.lua b/lua/bearded/plugins/lualine.lua
index 7290b6e..1b1e547 100644
--- a/lua/bearded/plugins/lualine.lua
+++ b/lua/bearded/plugins/lualine.lua
@@ -12,27 +12,32 @@ function M.highlights(palette)
   return {
     normal = {
       a = { bg = accent, fg = bg, gui = "bold" },
-      b = { bg = ui.primaryalt or bg, fg = accent },
+      b = { bg = ui.defaultalt or bg, fg = accent },
       c = { bg = bg, fg = fg },
     },
     insert = {
       a = { bg = colors.green or accent, fg = bg, gui = "bold" },
-      b = { bg = ui.primaryalt or bg, fg = colors.green or accent },
+      b = { bg = ui.defaultalt or bg, fg = colors.green or accent },
       c = { bg = bg, fg = fg },
     },
     visual = {
       a = { bg = colors.purple or accent, fg = bg, gui = "bold" },
-      b = { bg = ui.primaryalt or bg, fg = colors.purple or accent },
+      b = { bg = ui.defaultalt or bg, fg = colors.purple or accent },
       c = { bg = bg, fg = fg },
     },
     replace = {
       a = { bg = colors.red or accent, fg = bg, gui = "bold" },
-      b = { bg = ui.primaryalt or bg, fg = colors.red or accent },
+      b = { bg = ui.defaultalt or bg, fg = colors.red or accent },
       c = { bg = bg, fg = fg },
     },
     command = {
       a = { bg = colors.yellow or accent, fg = bg, gui = "bold" },
-      b = { bg = ui.primaryalt or bg, fg = colors.yellow or accent },
+      b = { bg = ui.defaultalt or bg, fg = colors.yellow or accent },
+      c = { bg = bg, fg = fg },
+    },
+    terminal = {
+      a = { bg = colors.greenAlt or accent, fg = bg, gui = "bold" },
+      b = { bg = ui.defaultalt or bg, fg = colors.greenAlt or accent },
       c = { bg = bg, fg = fg },
     },
     inactive = {
